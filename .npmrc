# Refuse to install on a Node older than BACKEND.md §0 requires.
engine-strict=true

# Workspace packages are always linked from this repository, never fetched.
link-workspace-packages=true
prefer-workspace-packages=true

# A peer-dependency mismatch is a real problem; do not paper over it.
strict-peer-dependencies=true
