// pg-boss subscriber registration and the cron schedule (BACKEND.md §8).
//
// Deployed as a separate Render service from the same image (BACKEND.md §12).
// First jobs arrive with step 11 (feat/notifications).
export {};
