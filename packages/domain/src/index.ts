// Public surface of the shared domain (BACKEND.md §2).
//
// Built in step 2 (feat/domain-queue): types, events, reducer, eta, rate,
// rules, replay. Nothing downstream of the reducer starts until it is green.
export {};
