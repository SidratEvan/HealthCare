// Design system (FRONTEND.md §1-6).
//
// Built in step 7 (feat/ui-tokens): colour and type tokens first, then Button,
// Input, OTP, Card, Chip, Sheet and Toast, then the signature components —
// <LiveSerialCard>, <FreshnessLine>, <QueueTable>, <BedTile>, <CapacityMirror>.
// Nothing else starts until these exist, or the design fragments (FRONTEND.md §14).
export {};
